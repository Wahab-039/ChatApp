apiVersion: v1
kind: Service
metadata:
  name: chatapp-api-service
  namespace: chatapp
spec:
  selector:
    app: chatapp-api
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
